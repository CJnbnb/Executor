<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.executor.xxljobexecutormqimprove.mapper.TaskEventLogMapper">

    <insert id="insert" parameterType="com.executor.xxljobexecutormqimprove.model.entity.TaskEventLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_event_log (task_id, task_name, biz_name, biz_group, event_type, from_status, to_status, event_msg, event_time)
        VALUES (#{taskId}, #{taskName}, #{bizName}, #{bizGroup}, #{eventType}, #{fromStatus}, #{toStatus}, #{eventMsg}, #{eventTime})
    </insert>

    <insert id="batchInsert">
        INSERT INTO task_event_log (task_id, task_name, biz_name, biz_group, event_type, from_status, to_status, event_msg, event_time)
        VALUES
        <foreach collection="list" item="log" separator=",">
            (#{log.taskId}, #{log.taskName}, #{log.bizName}, #{log.bizGroup}, #{log.eventType}, #{log.fromStatus}, #{log.toStatus}, #{log.eventMsg}, #{log.eventTime})
        </foreach>
    </insert>

</mapper>
