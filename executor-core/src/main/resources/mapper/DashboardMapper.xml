<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.executor.xxljobexecutormqimprove.dashboard.mapper.DashboardMapper">

    <select id="countStats" resultType="map">
        SELECT
            COUNT(*)                                                      AS total,
            SUM(CASE WHEN enable = '1' THEN 1 ELSE 0 END)               AS enabled,
            SUM(CASE WHEN next_trigger_time &lt; #{now} AND enable = '1' THEN 1 ELSE 0 END) AS pending,
            SUM(CASE WHEN enable = '0' THEN 1 ELSE 0 END)               AS disabled,
            SUM(CASE WHEN process = 'processing' THEN 1 ELSE 0 END)     AS processing,
            SUM(CASE WHEN process = 'processing' AND update_at &lt; DATE_SUB(NOW(), INTERVAL 5 MINUTE) THEN 1 ELSE 0 END) AS stuck
        FROM user_scheduled_common_task
    </select>

    <select id="selectTasksPage" resultType="com.executor.xxljobexecutormqimprove.entity.CommonTaskEntity">
        SELECT * FROM user_scheduled_common_task
        WHERE 1 = 1
        <if test="taskName != null and taskName != ''">
            AND task_name LIKE CONCAT('%', #{taskName}, '%')
        </if>
        <if test="bizName != null and bizName != ''">
            AND biz_name LIKE CONCAT('%', #{bizName}, '%')
        </if>
        <if test="bizGroup != null and bizGroup != ''">
            AND biz_group LIKE CONCAT('%', #{bizGroup}, '%')
        </if>
        <if test="enable != null and enable != ''">
            AND enable = #{enable}
        </if>
        <if test="process != null and process != ''">
            <choose>
                <when test="process == 'pending'">
                    AND process IS NULL
                </when>
                <when test="process == 'stuck'">
                    AND process = 'processing' AND update_at &lt; DATE_SUB(NOW(), INTERVAL 5 MINUTE)
                </when>
                <otherwise>
                    AND process = #{process}
                </otherwise>
            </choose>
        </if>
        ORDER BY next_trigger_time ASC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countTasks" resultType="long">
        SELECT COUNT(*) FROM user_scheduled_common_task
        WHERE 1 = 1
        <if test="taskName != null and taskName != ''">
            AND task_name LIKE CONCAT('%', #{taskName}, '%')
        </if>
        <if test="bizName != null and bizName != ''">
            AND biz_name LIKE CONCAT('%', #{bizName}, '%')
        </if>
        <if test="bizGroup != null and bizGroup != ''">
            AND biz_group LIKE CONCAT('%', #{bizGroup}, '%')
        </if>
        <if test="enable != null and enable != ''">
            AND enable = #{enable}
        </if>
        <if test="process != null and process != ''">
            <choose>
                <when test="process == 'pending'">
                    AND process IS NULL
                </when>
                <when test="process == 'stuck'">
                    AND process = 'processing' AND update_at &lt; DATE_SUB(NOW(), INTERVAL 5 MINUTE)
                </when>
                <otherwise>
                    AND process = #{process}
                </otherwise>
            </choose>
        </if>
    </select>

    <update id="toggleEnable">
        UPDATE user_scheduled_common_task
        SET enable = IF(enable = '1', '0', '1')
        WHERE id = #{id}
    </update>

    <update id="batchToggleEnable">
        UPDATE user_scheduled_common_task
        SET enable = #{enable}
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <delete id="deleteById">
        DELETE FROM user_scheduled_common_task WHERE id = #{id}
    </delete>

    <delete id="batchDeleteByIds">
        DELETE FROM user_scheduled_common_task WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="releaseTask">
        UPDATE user_scheduled_common_task SET process = NULL WHERE id = #{id}
    </update>

    <update id="batchReleaseTasks">
        UPDATE user_scheduled_common_task SET process = NULL WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
